# セキュリティの考え方

- シークレット、API キー、パスワード等は環境変数で管理（ハードコード禁止）
- すべての外部入力を検証
- 必要最小限の権限で動作（最小権限の原則）
- 不要な依存関係を避ける
- セキュリティ監査ツールを定期的に実行
- 以下の機密ファイル/ディレクトリを読み取り・編集・コミットしない
  - `.env` / `credentials*`
  - `~/.aws/*` / `~/.ssh/*`
  - `*.pem` / `*.key`
- `rm -rf /` や force push 等の破壊的コマンドを実行しない
